#image: alpine:3.6

pipelines:
  branches:
    yalla:
      - step:
          name: Build archive
          script:
            - ./ci/bitbucket-deploy.sh
            - export BITBUCKET_COMMIT=$BITBUCKET_COMMIT
