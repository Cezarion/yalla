#image: alpine:3.6

pipelines:
  branches:
    yalla:
      - step:
          name: Build archive
          script:
            - export BITBUCKET_COMMIT=$BITBUCKET_COMMIT
            - ./ci/bitbucket-deploy.sh
